步骤一:终端下操作:
sudo apt-get update# 更新软件包
sudo apt-get upgrade# 升级软件包
sudo apt-get font-manager# 安装字体管理器
sudo apt-get install fonts-firacode# 安装 firacode 字体

步骤二:vs内操作在settings.json中添加下面两行:
"editor.fontFamily": "Fira Code",
"editor.fontLigatures": true,

步骤三:重启

